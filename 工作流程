# 提单机器人部署流程

## 环节总览

① 本地项目（含 package.json）→ ② 上传到 `/opt/1panel/www/xiaos` → ③ 1Panel 运行环境启动（3000）→ ④ 反代 `xiaos.hnzhcyy.cn` → ⑤ 企微保存回调 URL

## ① 本地（已完成项目骨架）

```bash
cd C:\atool\zh-xiaos-tidan
npm install
npm start
```

浏览器或 curl：`http://127.0.0.1:3000/health`

## ② 上传服务器

将以下内容上传到 **`/opt/1panel/www/xiaos/`**（不要只传 .env）：

- package.json、package-lock.json
- src/
- .env

**不要上传** `node_modules/`（在服务器上执行 npm install）。

## ③ 1Panel 运行环境

| 配置 | 值 |
|------|-----|
| 项目目录 | `/opt/1panel/www/xiaos` |
| 启动命令 | `start` |
| 端口 | `3000` |

安装依赖 → 启动 → 日志见 `listening on 3000`

## ④ 网站反代（应已配置）

- 域名：`xiaos.hnzhcyy.cn`
- 代理：`http://127.0.0.1:3000`

## ⑤ 企微机器人

| 项 | 值 |
|----|-----|
| URL | `https://xiaos.hnzhcyy.cn/wecom/robot/callback` |
| Token / EncodingAESKey | 与 `.env` 一致 |

```bash
curl -i https://xiaos.hnzhcyy.cn/health
```

## ⑥ 后续

在 `src/wecom/handler.js` 接 LLM；新建 OA 客户端调 `data_create`。
